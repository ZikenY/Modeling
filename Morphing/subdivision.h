#if !defined(ZIKEN_SUBDIVISION)
#define ZIKEN_SUBDIVISION

#include "../../commoninclude/commoninclude.h"

void subdivision(const string src_m, int iteration_count = 1,
    bool convert_to_obj = false);

#endif
